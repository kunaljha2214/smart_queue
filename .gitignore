# Root of project: C:\Users\kunal\OneDrive\Desktop\smart_queue
# Ignores apply to everything under this folder (backend, mobile, etc.)

# Dependencies
node_modules/

# Environment & secrets (never commit)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build / output
dist/
build/
coverage/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Optional: local notes
*.local

# -----------------------------------------------------------------------------
# Mobile (Expo / React Native) — one Git repo at project root only (no nested mobile/.git)
# -----------------------------------------------------------------------------
mobile/.git/

# Expo / Metro
mobile/.expo/
mobile/web-build/
mobile/dist/
mobile/.metro-health-check*

# Android native (mobile/android)
mobile/android/app/build/
mobile/android/build/
mobile/android/.gradle/
mobile/android/local.properties
mobile/android/.idea/
mobile/android/*.iml
mobile/android/.cxx/

# iOS native (if you add mobile/ios later)
mobile/ios/Pods/
mobile/ios/build/
mobile/ios/*.xcworkspace/xcuserdata/
mobile/ios/*.xcodeproj/xcuserdata/

# Keystores / signing (never commit)
mobile/**/*.jks
mobile/**/*.keystore
